language: python
python:
  - '3.6'
before install:
  - wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda config --add channels conda-forge
  - conda config --add channels bioconda
  - conda install -c adamkoziol genewrappers=0.0.0.3=py_0
  - conda install scikit-learn=0.21.1=py36h22eb022_0
  - conda install scipy=1.3.0=py36h921218d_0
  - conda install prodigal=2.6.3=1
  - conda install biopython=1.72=py36_0

script:
  - pytest tests/ -s -vv
